#!/usr/bin/env bash

set -euo pipefail

cd "$(git rev-parse --show-toplevel)"

# shellcheck source=./bun.sh
source "$(cd "$(dirname "$0")" && pwd)/bun.sh"

echo "pre-commit: rodando lint (bun run lint)..." >&2
run_bun run lint
